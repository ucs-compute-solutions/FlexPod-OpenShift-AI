---
- name: Create Firmware Policy
  vars:
    api_info: &api_info
      api_private_key: "{{ intersight_api_private_key }}"
      api_key_id: "{{ intersight_api_key_id }}"
      api_uri: "{{ api_uri | default(omit) }}"
      validate_certs: "{{ validate_certs | default(omit) }}"
      state: "{{ state | default(omit) }}"

  cisco.intersight.intersight_rest_api:
    <<: *api_info
    resource_path: /firmware/Policies
    query_params:
       $filter: "Name eq '{{ name_of_firmware_policy }}'"
    api_body: {
          "Name": "{{ name_of_firmware_policy }}",
          "Description": "{{ description_of_firmware_policy }}",
          "Organization": {
             "Moid": "{{ intersight_org.api_response.Moid }}"
           },
          Tags: [{Key: "Provisioned by:", Value: "Intersight-Ansible"},{Key: "Project:", Value: "{{ prefix }}"}],
          ModelBundleCombo:
          [
            {
                ModelFamily: "{{ model_family }}", 
                BundleVersion: "{{ bundle_version }}"
            }
          ],
          TargetPlatform: "FIAttached"
        }
    state: present
  register: firmware_policy